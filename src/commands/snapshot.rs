// This is free and unencumbered software released into the public domain.

use clientele::{StandardOptions, SysexitsError};

pub fn snapshot(urls: &Vec<String>, flags: &StandardOptions) -> Result<(), SysexitsError> {
    todo!()
}
